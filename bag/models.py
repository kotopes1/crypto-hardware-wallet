"""
Django's database models module

This module contains the base `Model` class and related
tools for creating and manipulating database tables.
It provides a high-level, Pythonic interface for
interacting with the database.
This module also contains the fields that can be used
to define the columns of the database table.
"""
from django.db import models

# Create your models here.
